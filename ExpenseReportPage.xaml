<Page x:Class="WpfDocLearning.ExpenseReportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfDocLearning"
      mc:Ignorable="d" 
       d:DesignHeight="350" d:DesignWidth="500"
      Title="ExpenseReportPage">

    <Grid>
        <Grid.Resources>
            <DataTemplate x:Key="typeItemTemplate">
                <Label Content="{Binding XPath=@ExpenseType}"/>
            </DataTemplate>
            <DataTemplate x:Key="amountItemTemplate">
                <Label>
                    <Label.Style>
                        <Style TargetType="Label">

                            <Setter Property="Foreground"
                        Value="{Binding XPath=@ExpenseAmount,
                        Converter={StaticResource ExpenseAmountBrushConverter}}"/>

                            <Style.Triggers>
                                <DataTrigger Binding="{Binding XPath=@ExpenseAmount}" Value="6000">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                            </Style.Triggers>

                        </Style>
                    </Label.Style>
                    <Label.Content>
                        <PriorityBinding>
                            <!-- Primary binding -->
                            <Binding XPath="@ExpenseAmount"/>

                            <!-- Fallback if amount is missing -->
                            <Binding Source="N/A"/>
                        </PriorityBinding>
                    </Label.Content>
                </Label>
            </DataTemplate>
        </Grid.Resources>
        <Grid.Background>
            <ImageBrush ImageSource="/watermark.png"/>
        </Grid.Background>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="230"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Label Grid.Column="1" Style="{StaticResource headerTextStyle}">
            Expense Report for:
        </Label>
        <Grid Margin="10" Grid.Column="1" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition />
            </Grid.RowDefinitions>

            <StackPanel Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal">
                <Label Style="{StaticResource labelStyle}">
                    Name:
                </Label>
                <Label Style="{StaticResource labelStyle}" Content="{Binding XPath=@Name}"></Label>
            </StackPanel>

            <StackPanel Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal">
                <Label Style="{StaticResource labelStyle}">
                    Department:
                </Label>
                <Label Style="{StaticResource labelStyle}" Content="{Binding XPath=@Department}"></Label>
            </StackPanel>

            <Grid Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="2" VerticalAlignment="Top" HorizontalAlignment="Left">

                <DataGrid AutoGenerateColumns="False" ColumnHeaderStyle="{StaticResource columnHeaderStyle}" ItemsSource="{Binding XPath=Expense}" RowDetailsVisibilityMode="VisibleWhenSelected">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="ExpenseType" CellTemplate="{StaticResource typeItemTemplate}" />
                        <DataGridTemplateColumn Header="Amount" CellTemplate="{StaticResource amountItemTemplate}" />
                    </DataGrid.Columns>
                    <DataGrid.RowDetailsTemplate>
                        <DataTemplate>
                            <Border Padding="8" Background="#FFF5F5F5">
                                <StackPanel>
                                    <TextBlock Text="Expense Details" FontWeight="Bold"/>

                                    <TextBlock
                    Text="{Binding XPath=@ExpenseType,
                                   StringFormat=Type: {0}}"/>

                                    <TextBlock
                    Text="{Binding XPath=@ExpenseAmount,
                                   StringFormat=Amount: {0}}"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </DataGrid.RowDetailsTemplate>
                </DataGrid>
            </Grid>
        </Grid>
    </Grid>
</Page>